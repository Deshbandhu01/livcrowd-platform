spring:
  profiles:
    active: default
  application:
    name: livcrowd-backend
  datasource:
    url: jdbc:mysql://localhost:3306/livcrowd?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: livcrowd123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

livcrowd:
  jwt:
    secret: ${JWT_SECRET:8a92fbd850a9b895fd8216fc1bffd8449c2889eb6e21ab1713e51241154c15da}
    expiry-ms: 86400000
  crowd:
    snapshot-interval-sec: 30
    smoothing-alpha: 0.4
